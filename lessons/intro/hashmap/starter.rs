use std::collections::HashMap;

/// Return the most frequent element in the slice.
/// If there is a tie, return the one that appeared first.
///
/// Guidance:
/// - Use a `HashMap<i32, usize>` for counts
/// - Track first-seen order separately (e.g., a `Vec<i32>`)
pub fn most_frequent(nums: &[i32]) -> Option<i32> {
    // Starter: compile-friendly placeholder. Replace with a real solution.
    nums.first().copied()
}



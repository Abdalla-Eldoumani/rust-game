/// Use Tokio mpsc to send numbers from multiple tasks and collect them.
/// Return them sorted.
pub async fn fan_in(workers: usize) -> Vec<usize> {
    // Starter placeholder
    (0..workers).collect()
}


